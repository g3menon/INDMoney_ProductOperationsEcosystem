import { EmptyState } from "@/components/shared/EmptyState";

export function ProductTab() {
  return (
    <EmptyState
      title="Weekly Pulse arrives in Phase 2"
      description="The Product tab will render pulse history, subscriptions, and analytics. Phase 1 only validates backend connectivity and badge plumbing."
    />
  );
}
